# SpringPet
This is a pet project using Spring framework with Cucumber integration.

To run tests properly, add JUnit configuration with following:
    -ea
    -Dconfig.location=classpath:application.yml
    -Dspring.profiles.active=chrome